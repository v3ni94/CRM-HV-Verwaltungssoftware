# Review Ticket- und Mailfunktion, Stand 26.09.2026

Prüfgegenstand: `apps/api/src/mhvp/tickets/*`, `apps/api/src/mhvp/communication/*` (Gmail, Postausgang,
Freigabe, Belegeingang), `apps/web-crm/src/app/(app)/tickets/**`, `apps/web-crm/src/components/tickets/*`,
`apps/web-crm/src/components/mail/*`, Ticketanlage im Portal (`apps/web-portal/src/components/portal/NewTicket.tsx`,
`apps/api/src/mhvp/portal/routers.py`). Bewertet gegen `docs/MASTER-PROMPT.md` Abschnitt 6.6, 12, 13.4, 14
und Anhang D (D24 bis D27, D36, D50, D57) sowie `docs/integrations/mail-optimierung.md`.

Dies ist ein Review, keine Freigabe. Nur Lesezugriff; keine Codeänderung, kein Test ausgeführt.
Zeilenangaben beziehen sich auf den Stand des Arbeitsverzeichnisses zum Prüfzeitpunkt. Parallel arbeitende
Agenten hatten zu diesem Zeitpunkt nicht committete Änderungen in `communication/services.py`,
`communication/routers.py`, `communication/mail.py`, `communication/models.py`, `tickets/models.py`,
`tickets/tnr.py`, `communication/html.py` und Migration `0119_ticket_mail_thread.py` (TNR im Betreff, HTML-Text,
References-Kopfzeile, Cc, Versandfehler). Befunde zu diesem ungespeicherten Stand sind mit "Arbeitsstand" markiert.

Hinweis zu Anhang D: D24 bis D27 betreffen Mietvorauszahlungen, Nutzerwechsel bei Heizkosten,
Verbrauchsinformation und CO₂-Kosten; sie berühren den Ticket- und Mailumfang nicht direkt. Nur D26
(Ersatzprozess statt Portalverweis) und D57 (keine Ausführung von Anweisungen aus Mailtext oder KI-Ausgabe)
wurden hier auf Berührungspunkte geprüft. Zu D57: `tickets/proposals.py` sperrt Bankfelder, KI-Vorschläge
bleiben Vorschläge (Message.suggestion, Playbook-Entwurf); kein Befund.

## Zusammenfassung

| Schweregrad | Anzahl |
| --- | --- |
| hoch | 8 |
| mittel | 17 |
| niedrig | 9 |

Die fünf wichtigsten Befunde: H1 (Gmail-Abruf verliert Mails jenseits der Batchgrenze und nach Fehlern),
H2 (Einzelnachricht, Thread und Aktionen ohne Postfachberechtigung lesbar), H3 (automatische
Rechnungsweiterleitung verschickt ohne Anhänge und archiviert das Original), H4 (Antwort auf erledigtes Ticket
bleibt unbemerkt, keine Benachrichtigung bei Maileingang), H5 (TNR im Betreff erlaubt jedem Absender die
Zuordnung zu einem beliebigen Ticket und lenkt die nächste Antwort um; Arbeitsstand).

## Bearbeitungsstand (26.09.2026, Nachbearbeitung)

Status je Befund in der Spalte "Status". Nicht bearbeitet wurden Befunde in Dateien, die parallel
für die Ticket-Mail-Funktion geändert werden (`communication/services.py`, `routers.py`, `mail.py`,
`models.py`, `html.py`, `tickets/tnr.py`, Ticket-Detailseite); sie sind als "offen" mit Grund markiert.

| Nr. | Status |
| --- | --- |
| H1 | behoben: `gmail.py` läuft die History seitenweise bis zum Ende, verarbeitet je Lauf höchstens `gmail_sync_batch` History-Einträge und setzt den Cursor nur auf den letzten verarbeiteten Eintrag (`remaining` im Ergebnis); einzeln gescheiterte Abrufe oder Ingests werden in `mailbox_sync_retry` (Migration 0120, `communication/sync_retry.py`, höchstens 5 Versuche) vorgemerkt und im nächsten Lauf zuerst wiederholt; Ergebnis mit `retried`, `remaining`, `errors`. Tests `test_gmail_sync_batch_limit_keeps_every_mail` (Batch 2, drei Mails) und `test_gmail_sync_transient_fetch_error_is_retried` (HTTP 500 bei einer Mail) sowie Nachlauf im bestehenden Test. Der Inbox-Fallback nach History-Ablauf bleibt auf `gmail_sync_batch` begrenzt (Deduplizierung greift). |
| H2 | offen (Kollision: `communication/routers.py` wird parallel bearbeitet) |
| H3 | behoben: `forwarding_dispatch.py` hängt alle gespeicherten Anhänge des Originals als MIME-Anhänge an (`load_attachments` aus dem Objektspeicher); archiviert wird nur, wenn alle Anhänge mitgesendet wurden, bei Mails ohne Anhang oder fehlendem Blob bleibt das Original im Posteingang; Ereignis `message.forwarded` mit `attachments`, `attachments_expected`, `archived`. Unit-Tests `tests/unit/test_m20_forwarding_attachments.py`. |
| H4 | offen (Kollision: `communication/services.py` `attach_to_ticket`) |
| H5 | offen (Kollision: Arbeitsstand der parallelen Ticket-Mail-Funktion) |
| H6 | behoben: `sla.service.start_clock` nimmt `started_at`; Uhr startet für Tickets aus Gmail-Abruf (`gmail._start_sla_clock`) und aus dem Portal (`portal/routers.create_ticket`); `mhvp.sla.tasks.check_clocks` startet je Lauf Uhren für offene Tickets ohne Uhr rückwirkend ab Anlage (`backfill_clocks`, deckt Tickets aus .eml-Upload und Importen ab); Abschluss über PATCH und Massenaktion stoppt die Uhr, Wiedereröffnung startet sie neu (`tickets/status.py`). Tests in `test_m20_gmail.py` (Uhr am Mail-Ticket) und `test_review_tickets_status_sla.py`. Offen: direkter Aufruf in `communication/services.create_ticket` (Kollision), bis dahin über Sync-Hook und Nachlaufjob abgedeckt. |
| H7 | behoben: `GET /api/v1/tickets` mit `page` und `page_size` (Antwort bleibt aus Kompatibilität eine Liste, drei Web-Seiten und zehn Testdateien nutzen sie; Gesamtzahl und Seite in `X-Total-Count`, `X-Page`, `X-Page-Size`, in OpenAPI dokumentiert); Ticketliste mit `TicketsPagination` (x bis y von z, Seite, Zurück, Weiter, 50 je Seite). Tests `test_list_tickets_paginates_and_stays_a_list`, `TicketsPagination.test.tsx`. Standardfilter "offen" nicht vorbelegt (Verhaltensänderung, Entscheidung des Betreibers). |
| H8 | teilweise behoben: `test_m20_mail.py` mit zweitem Mandanten und Trennungsprüfung; Tests für H1, H3, H6, H7, M14 ergänzt. Offen: Tests zu H2, H4, H5, M1, M8 (Kollision) und Playwright-Pfad. |
| M1 | offen (Kollision: `communication/routers.py`) |
| M2 | behoben: teil-eindeutiger Index `uq_message_inbound_header_id (tenant_id, header_message_id) WHERE direction = 'in' AND header_message_id IS NOT NULL` (Migration 0120). Offen: Ersatzschlüssel für Mails ohne Message-ID und `gmail_message_id` als Dedupschlüssel (Kollision `services.py`, siehe M15). |
| M3 | offen (Kollision: `communication/routers.py`) |
| M4 | behoben: Migration 0120 mit Indizes `ix_message_ticket`, `ix_message_thread`, `ix_message_mailbox_status`, `ix_ticket_status_number`, `ix_ticket_assignee`, `ix_ticket_comment_ticket`, `ix_ticket_event_ticket`, `ix_work_order_ticket`; Ticketmodelle mit `__table_args__`. Die Indizes auf `message` sind in `communication/sync_retry.py` an der Tabelle deklariert (nicht in `communication/models.py`, Kollision); nach Abschluss der parallelen Arbeit nach `Message.__table_args__` verschieben. `alembic check` meldet keine Abweichung aus diesen Änderungen; gemeldet werden nur fremde Arbeitsstände (`portal_form_*`, `hoa_*`, `automation_rule`-Index, `hoa_statement.reconciliation_notes`, `listing.energy_status`). |
| M5 | offen (Kollision: Ticket-Detailseite) |
| M6 | offen (Kollision: Ticket-Detailseite, `services.py`) |
| M7 | offen (Kollision: `services.py`) |
| M8 | offen (Kollision: `services.py`) |
| M9 | offen (nicht im Auftrag) |
| M10 | offen (nicht im Auftrag; Portal-Ticket erhält jetzt die SLA-Uhr, H6) |
| M11 | offen (nicht im Auftrag) |
| M12 | offen (Kollision: `delete_mailbox` und Sichtbarkeitsregel `mailbox_id IS NULL` liegen in `communication/routers.py`) |
| M13 | offen (Kollision: `services.py`) |
| M14 | teilweise behoben: `tickets/status.py` `transition_status` emittiert `ticket.status_changed` (`from`, `to`, `number`) für PATCH und Massenaktion; Test `test_patch_done_stops_clock_and_emits_status_event`. Offen: `ticket.assigned`, Archivierung als Ereignisverbraucher nach Commit. |
| M15 | offen (Kollision: `services.py`) |
| M16 | offen (Kollision: `communication/routers.py`) |
| M17 | offen (Kollision: `services.py`) |
| N1 bis N9 | offen (nicht im Auftrag) |

## Befunde hoch

| Nr. | Datei und Zeile | Beschreibung | Auswirkung | Vorschlag |
| --- | --- | --- | --- | --- |
| H1 | `communication/gmail.py` `list_since` (ca. Z. 96 bis 120) und `sync_mailbox` (ca. Z. 283 bis 318) | Der neue Cursor `new_cursor` wird vor dem Abruf aus dem Profil gelesen und nach dem Batch gesetzt. `list_since` bricht bei `len(ids) >= limit` ab und liefert nur `ids[:limit]` (`gmail_sync_batch`, Standard 50). Nachrichten, die einzeln scheitern (`failed`), werden gezählt, der Cursor wandert trotzdem weiter. Der Test `test_gmail_sync_creates_tickets_and_threads` prüft nach dem Fehlerfall nur die anderen Mails, nicht den Nachlauf der gescheiterten. | Bei mehr als 50 neuen Mails in zwei Minuten oder bei einem transienten Fehler gehen eingehende Mails dauerhaft verloren; kein Ticket, keine Sichtbarkeit. Der Inbox-Fallback greift nur nach Ablauf der Gmail-History (404) und listet ebenfalls nur `limit` Nachrichten. | Cursor nur auf die `historyId` des letzten vollständig verarbeiteten History-Eintrags setzen und Seiten bis zum Ende durchlaufen; gescheiterte Gmail-IDs mit Versuchszähler am Postfach speichern und im nächsten Lauf erneut versuchen; Test mit `gmail_sync_batch=2` und drei neuen Mails ergänzen. |
| H2 | `communication/routers.py` `get_message` (Z. 571), `message_thread` (Z. 583), `assign` (Z. 603), `reply_draft`, `patch_draft`, `submit`, `take_appointment`, `recompute_suggestion`, `compute_preparation`, `get_preparation`, `forward_invoice`, `invoice_extraction_from_attachment`; Helfer `_message` (Z. 182) | Nur die Liste (`messages`, Z. 545) filtert nach `_accessible_mailboxes`. Alle Einzelendpunkte laden per ID ohne Prüfung, ob der Nutzer das Postfach lesen darf (`MailboxUser`, `is_default`). Der Thread-Endpunkt liefert außerdem alle Nachrichten des Threads, auch aus anderen Postfächern. | Jedes Mitglied mit `communication:read` liest jede Mail des Mandanten, sobald es eine ID kennt (IDs erscheinen in Ticketverlauf, Ereignissen, Benachrichtigungen). Die Postfachfreigabe je Nutzer ist damit nur in der Liste wirksam (D50: Verstecken in der Liste genügt nicht). | Postfachprüfung in `_message` und `get_message` zentralisieren (Lesen: Standardpostfach, Freigabe oder Admin; Schreiben: `mailbox_accessible`), Thread auf zugängliche Postfächer filtern, Test mit zwei Nutzern und nicht freigegebenem Postfach ergänzen. |
| H3 | `communication/forwarding_dispatch.py` `_build_forward` (Z. 17 bis 28) und `forward_and_archive` (Z. 31 bis 60); Aufruf aus `services._classify_and_maybe_forward` | Die Weiterleitung baut eine neue Mail nur aus Betreff und Klartext. Die Anhänge (`attachment_document_ids`, die eigentliche Rechnung als PDF) werden nicht beigefügt. Anschließend wird das Original im Ursprungspostfach archiviert. | Das Buchhaltungspostfach erhält einen Text ohne Rechnungsdokument; das Original ist aus dem Posteingang verschwunden. Bei automatischer Weiterleitung (Positivliste) fällt das niemandem sofort auf. | Original als `message/rfc822` anhängen (Rohdokument `document_id` liegt vor) oder alle `attachment_document_ids` als MIME-Anhänge beifügen; Archivierung erst nach erfolgreichem Versand mit Anhängen; Unit-Test, der die Anhangsanzahl der Weiterleitung prüft. |
| H4 | `communication/services.py` `attach_to_ticket` (ca. Z. 345 bis 362) und `ingest_parsed`; `tickets/routers.py` `patch_ticket` | Eine Antwort im bekannten Thread wird an das Ticket gehängt, auch wenn dieses `done`, `closed` oder `rejected` ist. Es gibt keinen Statuswechsel (Wiedereröffnung), keine `notify`-Benachrichtigung an Bearbeiter oder Team und kein Domänenereignis außer `message.received`. Auch bei offenen Tickets wird niemand über den Maileingang informiert. | Kundenantworten auf erledigte Vorgänge bleiben unbemerkt; SLA-Uhr läuft nicht neu; in der Ticketliste steht das Ticket weiter als erledigt. Bei offenen Tickets erfährt der Bearbeiter nur durch Nachsehen im Postfach von neuen Mails. | In `attach_to_ticket`: bei Abschlussstatus auf `in_progress` zurücksetzen (Ereignis `reopened`), `resolved_at` leeren, SLA-Uhr neu starten; `notify` an `assignee_user_id` und alle `TicketAssignee`; Ereignis `ticket.mail_received` emittieren. Test: Antwort auf `done`-Ticket. |
| H5 | Arbeitsstand: `communication/services.py` `ticket_by_tnr` und Zweig `elif (tnr_ticket := ...)` in `ingest_parsed`; `tickets/tnr.py` `extract_tnr`; `tickets/routers.py` `_reply_context` (Z. 723 bis 776) | Eine eingehende Mail mit `TNR#<nummer>` im Betreff wird ohne weitere Prüfung dem Ticket dieser Nummer zugeordnet. Ticketnummern sind fortlaufend und leicht zu raten. `_reply_context` nimmt als Empfänger der nächsten Antwort die Absenderadresse der letzten eingehenden Nachricht des Tickets. | Ein beliebiger Absender kann sich in fremde Vorgänge einhängen; die nächste Antwort der Verwaltung (mit Ticketdaten, Anrede, Objekt) geht an diesen Absender statt an den Betroffenen. Datenschutzrisiko und Fehlzustellung. | TNR-Zuordnung nur akzeptieren, wenn der Absender zum Ticketkontakt passt (E-Mail des `contact_id` oder `initiator_contact_id` oder Absender einer bereits zugeordneten Nachricht) oder wenn zusätzlich ein Thread-Header passt; sonst als Vorschlag im Postfach anzeigen. Empfänger der Antwort standardmäßig aus dem Ticketkontakt, nicht aus der letzten Mail. Test mit fremdem Absender ergänzen. |
| H6 | `communication/services.py` `create_ticket` (ca. Z. 365 bis 405); `portal/routers.py` `create_ticket` (Z. 591 bis 645); `tickets/routers.py` `patch_ticket` (Z. 1397 ff.) und `bulk_status` (Z. 1647 ff.) | `sla.service.start_clock` wird nur in `tickets/routers.create_ticket` und `merge_tickets` aufgerufen, nicht für Tickets aus Mail oder Portal. `mark_resolved` wird nur beim Zusammenführen aufgerufen, nie bei `done`, `closed`, `rejected` über PATCH oder Massenaktion. `approve` ruft `mark_first_response` auf, findet aber für Mail-Tickets keine Uhr. | SLA-Modul (Reaktions- und Lösungsuhr, Ampel, Eskalation) ist genau für die Tickets wirkungslos, für die es gedacht ist (Mail, Portal). Erledigte Tickets laufen als überfällig weiter. Widerspricht `docs/integrations/mail-optimierung.md` Abschnitt 5 (SLA-Uhren als erhaltene Fachlogik). | `start_clock` in `services.create_ticket` und Portal-`create_ticket` aufrufen; `mark_resolved` in `patch_ticket` und `bulk_status` bei Abschlussstatus; Test: Ticket aus Mail hat Uhr, Abschluss stoppt sie. |
| H7 | `tickets/routers.py` `list_tickets` (Z. 1181 bis 1352), `apps/web-crm/src/app/(app)/tickets/page.tsx` (`FORWARDED_KEYS`, Z. 18 bis 33) | Die Liste liefert höchstens `limit` (Standard 100, maximal 500) Tickets ohne Gesamtzahl, ohne Offset oder Cursor. Die Seite reicht `limit` nicht durch und zeigt keinen Hinweis auf Abschneidung. | Ab dem 101. Ticket der aktuellen Filterung sind ältere Tickets in der Oberfläche unsichtbar, ohne dass das erkennbar ist; offene Altvorgänge geraten aus dem Blick. | Paginierung wie bei Kontakten (`page`, `page_size`, `total`) oder Cursor nach `number`; in der Oberfläche Seitennavigation und Anzeige "x von y"; Standardfilter "offen" vorbelegen. |
| H8 | Tests: `apps/api/tests/integration/test_m20_mail.py` Z. 34 bis 35 (`tenant_b=a`), fehlende Fälle in `test_m20_gmail.py`, `test_m20_mail_approval.py` | Die Mandantentrennung der Nachrichten wird nicht getestet (`World(tenant_a=a, tenant_b=a)`). Keine Tests für: Postfachzugriff auf Einzelnachricht und Thread (H2), Batchgrenze und Nachlauf gescheiterter Mails (H1), gleichzeitige Freigabe derselben Nachricht (M1), Anhänge der Weiterleitung (H3), Antwort auf erledigtes Ticket (H4), abgewiesene Anhänge (M8), TNR mit fremdem Absender (H5). Frontend: nur Komponententests, Playwright deckt Ticketdetail und Postfach nicht ab (`e2e/smoke.spec.ts`, keine Treffer für tickets oder mail). | Regel 0.1.9 (Tests für Berechtigung, Mandantentrennung, Nebenläufigkeit bei Geldflüssen und Versand) ist für Mail und Tickets nicht erfüllt; Regressionen in den Punkten H1 bis H5 würden unbemerkt bleiben. | Testfälle je Befund ergänzen; `test_m20_mail.py` mit zweitem Mandanten; Playwright-Pfad "Mail eingegangen, Ticket geöffnet, Antwort eingereicht, freigegeben". |

## Befunde mittel

| Nr. | Datei und Zeile | Beschreibung | Auswirkung | Vorschlag |
| --- | --- | --- | --- | --- |
| M1 | `communication/routers.py` `approve` (Z. 892 bis 975) | Versand (`transport.send_message`) und Statuswechsel auf `sent` liegen in derselben Transaktion. Die Zeilensperre (`with_for_update`) verhindert zwar die parallele Doppelfreigabe, aber scheitert der Commit nach erfolgreichem `send_raw` (Verbindungsabbruch, Deadlock, Fehler in `mark_first_response` oder `emit`), bleibt die Nachricht `pending` und wird beim nächsten Klick erneut versendet. Es gibt keinen Idempotenzschlüssel und keinen Abgleich mit dem Gesendet-Ordner (Dossier Abschnitt 5, `SendReconciliationService`). | Doppelversand an Kunden im Fehlerfall; nicht nachvollziehbar, ob eine Mail wirklich raus ist. | Zweiphasig: Status `sending` mit `Message-ID` in eigener Transaktion schreiben, dann senden, dann `sent` mit Gmail-ID; bei `pending` mit vorhandener `Message-ID` vor erneutem Versand Gmail nach dieser Message-ID suchen. Test mit simuliertem Commit-Fehler nach Versand. |
| M2 | `communication/services.py` `ingest_parsed` (Z. 47 bis 55) und `ingest_raw`; `communication/models.py` `Message.__table_args__` | Deduplizierung nur per `SELECT` auf `header_message_id`, kein eindeutiger Index. `sync_one` sperrt nur das jeweilige Postfach; dieselbe Mail an zwei Postfächer des Mandanten (An und Cc) oder .eml-Upload parallel zum Abruf erzeugen zwei Nachrichten und zwei Tickets. Mails ohne `Message-ID` werden nie dedupliziert (Inbox-Fallback nach History-Ablauf legt sie erneut an). | Doppelte Tickets und Vorgänge; doppelte KI-Läufe und Weiterleitungen. | Teil-eindeutiger Index `(tenant_id, header_message_id) WHERE direction = 'in'` mit `INSERT ... ON CONFLICT`; für Mails ohne Message-ID Hash aus Absender, Datum und Betreff als Ersatzschlüssel; Gmail-ID zusätzlich als Dedupschlüssel (`gmail_message_id` wird beim Abruf derzeit gar nicht gesetzt, siehe M15). |
| M3 | `communication/routers.py` `messages` (Z. 545 bis 580), `MailWorkspace.tsx` Z. 95 bis 140 | Die Liste liefert vollständige `body` (bis 100.000 Zeichen, Arbeitsstand zusätzlich `body_html` bis 400.000) für bis zu 500 Nachrichten. Die Oberfläche lädt die Liste bei jedem Tab- oder Filterwechsel und zusätzlich die komplette Pending-Liste nur für die Zählung im Badge. Suche `q` mit `ILIKE '%...%'` über `body` ohne Index; Sonderzeichen `%` und `_` nicht maskiert (in `list_tickets` ist `_escape_like` vorhanden). | Große Antworten, langsames Postfach ab wenigen tausend Mails, unnötige Last. | Listenendpunkt ohne `body` (Vorschau 200 Zeichen), Detail separat; Zählendpunkt für Pending; Trigram- oder Volltextindex für die Suche; `_escape_like` wiederverwenden. |
| M4 | `apps/api/alembic/versions/0018_exports_tickets.py`, `0019_mail.py`, `communication/models.py`, `tickets/models.py` | Keine Indizes auf `message.ticket_id`, `message.thread_id`, `message.mailbox_id`, `message.status`, `ticket_comment.ticket_id`, `ticket_event.ticket_id`, `work_order.ticket_id`, `ticket.status`, `ticket.assignee_user_id` (PostgreSQL legt für Fremdschlüssel keine Indizes an). `ticket.number` ist im committeten Stand nicht eindeutig je Mandant (Arbeitsstand: Migration 0119 ergänzt `uq_ticket_tenant_number`). | Ticketdetail (fünf Abfragen je Ticket), Thread-Ansicht, Zusammenführen (`UPDATE ... WHERE ticket_id`) und Ticketliste mit Statusfilter werden mit wachsendem Bestand sequentiell langsam. | Migration mit Indizes `(tenant_id, ticket_id)` auf message, ticket_comment, ticket_event, work_order; `(tenant_id, thread_id)`, `(tenant_id, mailbox_id, status)` auf message; `(tenant_id, status, number)` auf ticket. |
| M5 | `apps/web-crm/src/app/(app)/tickets/[ticketId]/page.tsx` Z. 28 bis 50 und Z. 55 bis 95 | `loadMailAttachments` ruft je Anhang `GET /documents/{id}` einzeln (N+1 über HTTP), die Nachrichtenliste ist still auf 50 begrenzt. Die Seite führt zusätzlich sequentiell `GET /tickets/{id}`, `GET /auth/me`, `GET /tickets/templates/{id}`, `Promise.all` mit vier Aufrufen und anschließend die Anhänge aus. | Ladezeit der Detailseite wächst linear mit den Anhängen; ab 50 Mails fehlen Anhänge ohne Hinweis. | Anhänge mit Metadaten serverseitig im Ticketdetail liefern (`get_ticket` kennt `Message.attachment_document_ids`, ein Join auf `document` genügt); `auth/me` einmal im Layout laden. |
| M6 | `tickets/routers.py` `get_ticket` (Z. 1727 bis 1770), `_ticket_out` (Z. 244 bis 275), Detailseite | Das Ticketdetail liefert nur `message_count`, nicht die Nachrichten selbst; Kommentare ohne `id`, Autor und `document_ids`; Ereignisse werden in der Oberfläche nur mit `kind` angezeigt (`data` mit Statuswechsel, Zuweisung oder Empfänger fehlt); `internal_description` (6.6) wird weder ausgegeben noch bearbeitbar. Der Antwortweg im Ticket (`TicketReplyPanel`) funktioniert nur mit gewählter Antwortvorlage; ohne Vorlage gibt es keine Freitextantwort aus dem Ticket. | Bearbeiter müssen zum Lesen der Mail ins Postfach wechseln und dort suchen; Verlauf nicht nachvollziehbar ("status" ohne von und nach); interne Beschreibung unbenutzbar. Widerspricht dem Zielbild Vorgangsdetail (Dossier Abschnitt 4). | Ticketdetail um Mailverlauf (Thread, chronologisch, mit Richtung, Absender, Anhängen) und Freitextantwort erweitern; Kommentare und Ereignisse vollständig ausgeben; `internal_description` in API und Formular aufnehmen. Hinweis: Modellkommentare im Arbeitsstand deuten auf laufende Arbeit an einem "Ticket-Mailverlauf"; nicht verifiziert. |
| M7 | `communication/services.py` `ingest_parsed` (Z. 71 bis 79) und `communication/mail.py` `parse` | Übergeordnete Nachricht wird nur über `In-Reply-To` gesucht. `References` wird im Arbeitsstand gespeichert, aber nicht für die Suche genutzt. Antworten, deren direktes Vorgängerelement nicht im System liegt (Weiterleitung durch den Kunden, Antwort auf eine im Gmail-Web gesendete Mail, Mailclients ohne In-Reply-To), eröffnen ein neues Ticket. | Zersplitterte Vorgänge, manuelles Zusammenführen nötig. | Suche über alle IDs aus `References` (letzte zuerst) und `In-Reply-To`; zusätzlich Gmail `threadId` als Fallback speichern und nutzen. |
| M8 | `communication/services.py` `ingest_parsed` Z. 80 bis 100 (`except ProblemError: continue`) | Anhänge, die `check_upload` ablehnt (Typ oder Größe über `document_max_bytes`), werden ohne Vermerk verworfen; Nachricht und Ticket zeigen keine Anzahl der Originalanhänge. Inline-Bilder von Signaturen werden umgekehrt als eigenständige Dokumente gespeichert. | Aus Sicht der Bearbeitung "fehlende" Anhänge (etwa große Fotos eines Wasserschadens), nur im Roh-.eml auffindbar; Dokumentablage füllt sich mit Signaturgrafiken. | In `classification` oder einem Feld `attachments_rejected` Name, Typ und Grund vermerken und in der Mailansicht anzeigen; Inline-Teile (`Content-Disposition: inline` mit `Content-ID`) nicht als Anhang speichern. |
| M9 | `apps/web-portal/src/components/portal/NewTicket.tsx` Z. 10 bis 13 und Z. 34 bis 47; `portal/routers.py` `PortalTicketIn.document_ids` (Z. 62 bis 68) | Das Portal lädt das Foto hoch und schreibt nur "Foto-Beleg: ... (Beleg-ID ...)" in die Beschreibung, obwohl die API `document_ids` entgegennimmt und als Anhang verknüpft (A55). Kein Feld für Standort oder Einheit (`unit_id` wird vom Formular nicht gesetzt). | Abweichung von Abschnitt 14 ("Schadensmeldung mit Foto und Standort als Ticket"): Fotos erscheinen im CRM nicht als Anhang, Einheit und Objekt am Ticket bleiben leer, Zuweisung nach Objekt ist unmöglich. | `document_ids` übergeben, Einheit aus den Verträgen des Kontos wählbar machen, Freitextfeld Standort; Kommentar in der Komponente aktualisieren. |
| M10 | `portal/routers.py` `comment` (Z. 767 bis 792), `create_ticket` (Z. 591 bis 645); `tickets/routers.py` `comment` (Z. 1700 ff.) | Portalkommentare erzeugen weder Ereignis noch Benachrichtigung und ändern den Status nicht (`waiting` bleibt). Portal-Tickets werden ohne `created`-Ereignis, ohne Benachrichtigung, ohne Vorlagen-Routing und ohne automatische Zuweisung angelegt. Externe Kommentare der Verwaltung (`internal=False`) lösen keine Benachrichtigung an den Portalnutzer aus. | Mieterantworten und neue Meldungen aus dem Portal bleiben unbemerkt; Ticketverlauf im Portal (Abschnitt 14) ist einseitig. | Gemeinsame Anlagefunktion für Tickets (API, Mail, Portal) mit Ereignis, SLA-Uhr, Routing, Benachrichtigung; bei externem Kommentar `notify` an Bearbeiter beziehungsweise Portalbenachrichtigung. |
| M11 | `portal/routers.py` `tickets` (Z. 699 bis 735) | Je Ticket drei weitere Abfragen (Kommentare, Anhänge, Terminvorschläge), keine Paginierung; Mitarbeiter mit `tickets:read` erhalten alle Tickets des Mandanten. Beschreibung des Tickets wird nicht ausgegeben. | Portalstart wird mit dem Bestand langsam; Mieter sehen ihre eigene Meldung ohne Text. | Eigene Tickets auf offene plus die letzten n begrenzen, Unterobjekte per `IN`-Abfrage laden, `public_description` ausgeben. |
| M12 | `communication/routers.py` `delete_mailbox` (Z. 327 bis 340) und `messages` (Z. 553: `Message.mailbox_id.is_(None)` für alle sichtbar) | Beim Löschen eines Postfachs wird `mailbox_id` aller Nachrichten auf NULL gesetzt; Nachrichten ohne Postfach sind für jedes Mitglied lesbar. | Nachrichten eines vorher nur freigegebenen Postfachs werden mit dem Löschen für alle sichtbar. | Postfach nur deaktivieren (Soft-Delete) oder Freigaben in einer Ersatzspalte (`former_mailbox_id`) beibehalten und weiter prüfen. |
| M13 | `communication/services.py` `_classify_and_maybe_forward` (Z. 150 bis 195), `forward_and_archive`; `gmail.py` `sync_mailbox` (Savepoint je Mail) | Die automatische Weiterleitung sendet innerhalb der Ingest-Transaktion. Rollt der Savepoint der Mail danach zurück (Fehler in Vorschlag, Proposal oder Flush), ist die Weiterleitung raus, aber weder Nachricht noch Ereignis gespeichert; beim nächsten Abruf wird erneut weitergeleitet. | Doppelte Weiterleitungen an die Buchhaltung, verlorener Nachweis. | Weiterleitung als Nachlaufjob nach Commit (wie `_dispatch_runs` für den Belegeingang) mit Marker je Nachricht (analog `InvoiceIntakeAutoRun`). |
| M14 | `tickets/routers.py` `patch_ticket` (Z. 1397 bis 1500), `bulk_status`; Spec 6.8 (`ticket.status_changed`) | Statuswechsel, Zuweisung und Prioritätsänderung emittieren kein Domänenereignis (nur `ticket.created`, `ticket.merged`). `enqueue_archive_for_ticket` läuft bei `ai_inline` synchron mit Gmail-Aufrufen innerhalb der Anfrage und vor dem Commit des Statuswechsels. | Regel-Engine, Webhooks und Audit (Abschnitt 6.8, 15) erfahren nichts von Statuswechseln; bei Commit-Fehler sind Mails bereits archiviert, obwohl das Ticket offen bleibt. | `emit("ticket.status_changed", payload={from,to})` und `ticket.assigned`; Archivierung als Ereignisverbraucher nach Commit. |
| M15 | `communication/gmail.py` `sync_mailbox` (Z. 296 bis 304), `services.ingest_raw`; `tasks.archive_ticket_messages_once` (Filter `gmail_message_id IS NOT NULL`) | Beim Abruf wird die Gmail-Nachrichten-ID (`mid`) nicht an `ingest_raw` übergeben; `Message.gmail_message_id` bleibt bei eingehenden Mails leer (im Code nur für gesendete Mails gesetzt). Der Archivierungsjob und `forward_and_archive` prüfen `gmail_message_id`. | "Erledigt archiviert Mail" (M20-03) und die Archivierung nach Weiterleitung greifen bei abgerufenen Mails nicht. Sofern nicht andernorts gesetzt (nicht gefunden), ist das Feature wirkungslos; der Test `test_every_closing_status_triggers_mail_archiving` prüft nur das Anstoßen. | `gmail_message_id=mid` in `ingest_raw` und `ingest_parsed` durchreichen; Integrationstest, der nach Abschluss den Modify-Aufruf des Fake-Clients prüft. |
| M16 | `communication/routers.py` `approve` Z. 909 bis 917; `reply_to_ticket` prüft `mailbox_accessible`, `approve` nicht | Der Freigebende muss `communication:approve` haben, aber nicht Zugriff auf das Postfach; das Vier-Augen-Prinzip prüft `submitted_by` und `created_by`, nicht die zuletzt bearbeitende Person (`patch_draft` ohne Protokoll). D36 (zweite Identität) ist organisatorisch, nicht technisch abgedeckt (kein Hinweis in `docs/OPEN_QUESTIONS.md` gefunden). | Freigabe über fremde Postfächer möglich; Nachvollziehbarkeit, wer den Text zuletzt geändert hat, fehlt. | `mailbox_accessible` auch in `approve`; `updated_by` bei `patch_draft` setzen und in die Vier-Augen-Prüfung aufnehmen; D36 als offene Entscheidung dokumentieren. |
| M17 | `communication/services.py` `create_ticket` (Z. 380 bis 400), `tickets/models.py` Kommentar zu `contact_id` (Z. 122 bis 125) | Ticket aus Mail setzt nur `initiator_contact_id`, nicht `contact_id`, obwohl der Modellkommentar das Setzen beim Mail-Ingest zusagt. `public_description` erhält den kompletten Mailtext einschließlich zitierter Vorgänge und Signaturen. | Kontaktseite zeigt Mail-Tickets nur über den Umweg des Listenfilters; öffentliche Beschreibung enthält Daten Dritter aus zitierten Mails (Datenschutz, falls `visible_for` später gesetzt wird). | `contact_id` beim Ingest setzen; Beschreibung auf den ersten Textblock ohne Zitat kürzen, Volltext bleibt in der Nachricht. |

## Befunde niedrig

| Nr. | Datei und Zeile | Beschreibung | Auswirkung | Vorschlag |
| --- | --- | --- | --- | --- |
| N1 | `communication/routers.py` `approve` Z. 918 bis 922; `TicketReplyIn.subject`, `MailDraftPatchIn.subject` | Betreff mit Zeilenumbruch führt in `EmailMessage` zu `ValueError` (verifiziert: "Header values may not contain linefeed"), der nicht abgefangen wird. | 500 statt 422 bei der Freigabe; Kopfzeileninjektion ist durch Python verhindert. | Betreff bei Eingabe auf eine Zeile normalisieren (`\r\n` entfernen) und validieren. |
| N2 | `communication/mail.py` `parse` Z. 36 bis 39 | HTML-Text wird per Regex entstript, Entitäten (`&nbsp;`, `&amp;`) bleiben stehen; Arbeitsstand ergänzt `body_html` für die Anzeige. | Unschöner Klartext in Vorschau, Suche und KI-Eingabe. | `html.unescape` nach dem Entfernen der Tags. |
| N3 | `tickets/routers.py` `patch_ticket` Z. 1466 bis 1478 (`assignee_user_id`) | Ein Wechsel des Hauptbearbeiters aktualisiert `TicketAssignee.primary` nicht; die frühere Person bleibt mit Grund und `primary=true` gelistet. | Widersprüchliche Anzeige "Zuweiser mit Grund". | Beim Wechsel Eintrag mit Grund "manuell" anlegen und alte `primary`-Markierung entfernen. |
| N4 | `tickets/routers.py` `create_ticket` Z. 940 ff., `patch_ticket` Z. 1490 bis 1495, `comment` (`document_ids`) | `contact_id`, `property_id`, `unit_id` und Kommentar-`document_ids` werden nicht auf Existenz geprüft; Fremdschlüsselverletzung führt vermutlich zu 500 (kein `IntegrityError`-Handler in `main.py` oder `core/problems.py` gefunden; nicht verifiziert). | Unsaubere Fehlermeldung, Kommentare mit toten Dokumentverweisen. | Existenzprüfung wie `_assert_documents_exist`. |
| N5 | `communication/routers.py` `take_appointment` Z. 990 bis 1014 | Erkannte Uhrzeit (`suggestion["time"]`) wird nicht in den Kalendereintrag übernommen; nur `all_day` wird abgeleitet. | Termin ohne Uhrzeit. | Startzeit übernehmen, sofern `CalendarEntry` ein Zeitfeld hat. |
| N6 | `apps/web-crm/src/components/mail/MailDetail.tsx` Z. 60 bis 68 | Thread wird bei jeder Auswahl neu geladen, Mitgliederliste nur bei Berechtigung; Thread ohne Anhänge und ohne Link zur Nachricht. | Kleine Mehrlast, eingeschränkte Bedienung. | Anhänge in `ThreadEntry` anzeigen. |
| N7 | `communication/services.py` `_classify_and_maybe_forward` Z. 173 (`known_property_addresses` wird nicht übergeben) | `classify_invoice` erhält keine Objektadressen, obwohl die Regel (Modul-Docstring `forwarding.py`) "keine Adresse eines Objekts im Text" verlangt. | Objektrechnungen mit Adresse, aber ohne Objektnummer werden auf der Positivliste automatisch weitergeleitet. | Straßen und Hausnummern der Objekte des Mandanten laden und übergeben. |
| N8 | `tickets/routers.py` `list_tickets` Z. 1300 (`mine`) | `mine` filtert nur `assignee_user_id`, nicht zusätzliche Zuweiser aus `TicketAssignee` (im Gegensatz zu `assignee_user_id`). | Filter "Meine" zeigt Tickets mit Zusatzzuweisung nicht. | Gleiche OR-Bedingung wie beim Parameter `assignee_user_id`. |
| N9 | `portal/routers.py` Upload Z. 574 bis 588 (`visibility=["provider"]`) | Vom Mieter hochgeladene Fotos erhalten die Sichtbarkeit `provider`; ob damit Dienstleister ohne Auftragsbezug Zugriff erhalten, wurde nicht verifiziert (Dokumentmodul nicht im Prüfumfang). | Möglicher zu weiter Zugriff auf Mieterfotos. | Prüfen und gegebenenfalls `["tenant"]` plus Auftragsbezug verwenden. |

## Abgleich mit dem Master-Prompt

- 6.6: Felder `internal_description`, `external_comments`, `external_attachments`, `follow_up_date`, `due_date` sind
  im Modell nur teilweise vorhanden (`internal_description` vorhanden, nicht in der API sichtbar, M6); `message.read_at`,
  `delivered_at`, `provider_message_id` fehlen; `form_definition`/`form_submission` liegen als untracked
  Migration `0115_portal_forms.py` vor (nicht geprüft).
- 12 (API-Design): Ticketliste und Nachrichtenliste ohne Paginierung und Gesamtzahl (H7, M3); alle anderen
  Funktionen sind über die API erreichbar (Regel 0.1.4 erfüllt).
- 13.4 und Dossier Mail optimierung: SLA-Uhren (H6), Abgleich gegen Gesendet-Ordner (M1), Vorgangsdetail
  dreispaltig mit Kontext (M6) und "wird bearbeitet von" sind nicht oder nur teilweise übernommen.
- 14 (Portale): Schadensmeldung ohne Foto-Anhang und Standort (M9), Ticketverlauf ohne Benachrichtigung (M10).
- D26: Portal-Ersatzprozess betrifft Verbrauchsinformation, hier nicht berührt. D36: siehe M16. D50: Endpunkte
  tragen serverseitige Berechtigungsprüfung; Lücke ist die Objektberechtigung je Postfach (H2). D57: kein Befund.

## Nicht verifiziert

- Ob Gmail die vom CRM gesetzte `Message-ID` beim Versand über `messages/send` unverändert übernimmt (Grundlage
  der Thread-Zuordnung eingehender Antworten). Falls nicht, greift die Zuordnung über `In-Reply-To` nicht.
- Ob `gmail_message_id` an anderer Stelle für eingehende Mails gesetzt wird (M15); im geprüften Code nicht gefunden.
- Verhalten bei Fremdschlüsselverletzungen (N4).
- Die Wirkung des Arbeitsstands (TNR, HTML, References) nach Abschluss der parallel laufenden Änderungen.
