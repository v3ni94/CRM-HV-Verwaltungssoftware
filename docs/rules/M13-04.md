# M13-04 XRechnung für Verwalterhonorar-Rechnungen nur aus eingetragenen Mandantendaten

| Field | Content |
| --- | --- |
| ID | `M13-04` |
| Title | Verwalterhonorar-Rechnung als XRechnung (UBL 2.1, XRechnung 3.0): Nummer, Steuerdaten, Leitweg-ID und Zahlungskonto ausschließlich aus `TenantBillingSettings`, Firmendaten aus `TenantSettings.company`; fehlt ein Wert, ist die Erzeugung gesperrt |
| Scope | `mhvp.accounting.xrechnung`, `AdminFeeInvoice` (Migration 0094), `POST /accounting/admin-fees/{id}/invoice-issue`, `GET /accounting/invoices/{id}/xrechnung.xml`, `GET .../xrechnung/check`, `POST .../xrechnung/document`; alle Mandanten; nur Verwalterhonorar (Mahngebühren-Rechnungen bleiben Entwurf, M16-01) |
| Source status | Fachliche Umsetzung nach 13.5 (E-Rechnung erzeugen) und Betreiberentscheidung 25.09.2026 (M13-04). Formale Grundlage: EN 16931 Pflichtfelder und XRechnung 3.0 (KoSIT), technisch geprüft mit dem offiziellen Validator 1.5.0 und der Konfiguration `xrechnung_3.0.2_2024-10-31` (P05, nicht im Repository gebündelt). Ob und ab wann ein Mandant zur E-Rechnung verpflichtet ist, bleibt V12/Steuerberatung; keine Rechtsnorm zitiert |
| Acceptance case | D41 (formal valide): `tests/unit/test_m13_xrechnung.py`, `tests/integration/test_m13_xrechnung.py` (Happy Path, Sperren, Rechte, Mandantentrennung, Entwürfe 409, Ablage, Kleinunternehmer). Offizieller Validator nur mit `MHVP_KOSIT_DIR` (`scripts/kosit_validate.sh`), sonst als übersprungen gemeldet |
| Implementation | `mhvp.accounting.xrechnung` (`issue`, `load`, `build_xml`, `check_structure`, `assert_generation_allowed`), Problem-Codes MHVP-BILL-0005 (Leitweg-ID), MHVP-BILL-0006 (nicht ausgestellt), MHVP-BILL-0007 (IBAN), bestehende MHVP-BILL-0002/0003; Migration `0094_admin_fee_invoice` (Tabelle `admin_fee_invoice`, Spalte `tenant_billing_settings.payee_iban`) |
| Change reason | Aufgabe A12 der Lückenliste 26.09.2026: Abnahme M13 (XRechnung valide) war offen |

## Regeln

- Ausstellen friert Nummer, Datum, Beträge, Positionen und Schuldner in `admin_fee_invoice`
  ein; das XML wird auf Anfrage daraus erzeugt. Steuerkennungen werden nicht kopiert, sondern
  zur Laufzeit aus `TenantBillingSettings` gelesen (verschlüsselt, maskiert in der API).
- Mindest- und Höchsthonorar sind eigene Positionen (Anpassung), damit die Positionssumme immer
  dem Netto entspricht (BR-CO-10, BR-CO-13). Eine Kürzung ist Menge -1 mit positivem Preis
  (BR-27), nie ein negativer Preis.
- Regelbesteuert: Kategorie S mit dem Steuersatz des Honorars; Steuersatz 0 ist gesperrt, weil
  die Kategorie nicht bestimmbar wäre. Kleinunternehmer: Kategorie E mit dem Pflichthinweistext
  als Befreiungsgrund; ein Honorar mit Steuer ist gesperrt (MHVP-BILL-0003).
- Pflichtangaben, die XRechnung über EN 16931 hinaus verlangt und die im Mandanten fehlen
  können, sperren die Erzeugung mit Fehlertext statt Platzhalter: Leitweg-ID (BT-10, BT-49 mit
  Schema 0204), IBAN des Rechnungsstellers (BT-84, BR-61), E-Mail, Telefon und Ansprechpartner
  des Verkäufers (BT-34, BG-6, BR-DE-2/5/6/7), USt-IdNr. oder Registernummer (BR-CO-26).
- Zahlungsbedingung (BT-20) ist der neutrale Text "Fälligkeit gemäß Verwaltervertrag"; ein
  Zahlungsziel wird nicht erfunden (offen, siehe `docs/OPEN_QUESTIONS.md` M13-04).
- Die Strukturprüfung (`check_structure`) liefert Findings (Pflichtfelder, Summen); sie ersetzt
  nicht den offiziellen Validator. Eingangsrechnungen und Entwürfe antworten 409.
- Nichts wird versendet oder gebucht; die Umsatzbuchung des Honorars bleibt hinter G1.
