# M27 Marktreife: plan and result

Status: technical part implemented; G5 not fulfilled (2026-09-23). Source: MASTER-PROMPT
5.3, 6.8 (license, usage_counter), 18.0 (G5), 18 (M27).

* `mhvp.platform.licensing`, migration 0024 (price_list_entry, license, usage_counter as
  platform tables without RLS, allowlist in `tests/integration/test_rls.py`): price list
  without seeded prices, licences per module with quota and overlap check, monthly usage
  counters (API and job on the 1st), onboarding checklist and G5 readiness that only
  reports the gate and keeps all G5 evidence open.
* Test: `tests/integration/test_m27_licensing.py`.
* Not done and not doable in code: security, data protection and domain review, penetration
  test, actual procedure documentation (M27-02, M27-03). G5 stays closed for every tenant.

## M27-01 pricing structure (decided 24.09.2026)

* Price per unit, module and month with optional minimum (`license.min_monthly_amount`,
  migration 0029). `GET /platform/tenants/{id}/billing-preview?month=` computes net amounts per
  module from the usage counter (a licence counts if valid on any day of the month) and flags
  units over quota. Amounts, VAT and invoicing of licence fees stay open (M27-01).
