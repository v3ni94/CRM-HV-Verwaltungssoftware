# M16 Mahnwesen: plan and result

Status: preview, locks, approval, fee levels and Mahnbescheid preparation implemented
(2026-09-25); object overrides with inheritance and dunning letters as PDF drafts implemented
(2026-09-26); fee amounts and Basiszinssatz stay empty until the operator enters them, letter
dispatch (M16-02) still locked.
Source: MASTER-PROMPT 6.4, 6.9.10, 7.5, 13.1, 15.1, 18 (M16).
Acceptance: dunning run for all properties with approval; dunning blocks respected;
delivery evidence (open, M16-02).

## Decisions

* `mhvp.accounting.dunning`, migration 0016: dunning_settings (levels: level, minimum days,
  text; threshold; tenant default with property override), dunning_run, dunning_case (level,
  items, total, fee and interest always 0,00, status proposed or excluded with reason).
* Only the leading system may dun (6.9.10, 13.1): cases in a comparison ledger are excluded;
  approval refuses non leading ledgers and requires a second person.
* Celery `mhvp.accounting.dunning_run` on the 5th at 06:00 creates previews only.
* Migrations 0068 (schema), 0071 and 0072 (both same day fixes: the new tables were missing
  the `created_by`/`updated_by` and `created_at`/`updated_at` `server_default` columns every
  `TimestampMixin` table needs; caught by the integration test, not by review) (25.09.2026, V7
  teilweise entschieden): `dunning_settings` gets
  `fee_from_level` (nullable), `interest_enabled`, `interest_base_rate` (Basiszinssatz, operator
  maintained, no value shipped), `interest_spread` (Aufschlag); each level in `levels` may carry
  a nullable `fee_amount`. `dunning_case` gets `fee_entry_id` and `fee_invoice_draft_id`. New
  tables `dunning_fee_invoice_draft` and `dunning_mahnbescheid_prep` (docs/rules/M16-01.md).
* `POST /dunning-settings/presets` loads the V7 ladder (Zahlungserinnerung 7 Tage, always free;
  1. Mahnung 14 Tage; 2. Mahnung 28 Tage; 3. Mahnung/letzte Mahnung 42 Tage) with
  `fee_from_level = 2` and every `fee_amount` left empty; an optional `interest_profile`
  (`verbraucher`/`unternehmer`) only fills `interest_spread` (5 or 9 Prozentpunkte, § 288 BGB),
  never `interest_base_rate`. `PUT /dunning-settings` refuses `interest_enabled=true` without a
  maintained `interest_base_rate`.
* A fee stays inactive (amount 0,00, nothing posted) unless both `fee_from_level` is reached and
  that level's `fee_amount` is set; no default amount is ever assumed (0.1.3). Interest is
  computed only as an informational Nebenforderung for the Mahnbescheid preparation
  (`interest_amount_for`, a generic day count approximation, not a legal certification, 0.2);
  it is never booked automatically.
* On approval, a configured fee becomes a draft receivable (Sollstellung, `EntryKind.DUNNING_FEE`,
  status `draft`) on the debtor's account inside the claim holder's ledger (WEG for Hausgeld,
  Vermieter/Eigentümer for rent, matching the existing ledger-per-legal-entity model, 6.9.1).
  When the claim holder's legal entity is not `LegalEntityKind.MANAGER` (Hausverwaltung Müller
  GmbH), a linked `dunning_fee_invoice_draft` records the draft HVM outgoing invoice to that
  claim holder; when the claim holder is HVM itself, only the receivable is created. Both stay
  drafts behind G1 and the existing four eyes release (posting the receivable uses the generic
  `POST /ledgers/{id}/entries/{id}/post` endpoint, which already enforces a second person).
* `POST /dunning-cases/{id}/mahnbescheid-vorbereitung` (only at the highest configured level)
  creates a `dunning_mahnbescheid_prep` record (Antragsteller legal entity, Antragsgegner
  snapshot, Hauptforderung, Nebenforderungen from the recorded fee/interest, Zustelladresse,
  internes Aktenzeichen), status `in_vorbereitung` plus a `hinweis` field naming the pending
  review by a lawyer ("Vorbereitung, Prüfung durch Rechtsanwalt", moved out of `status` because
  that column is a short code field); `GET` returns the same export data. No PDF export yet
  (open point below); no filing, no legal claim of completeness, deadline hints are not
  computed here.

* M16-09 closed (25.09.2026): `POST /dunning-cases/{id}/mark-sent` (`channel: post|email|portal`,
  permission `accounting:approve`) records `delivered_at`/`delivery_channel` and sets
  `DunningCase.status = "sent"`, which is exactly what `last_level` needs to let the next
  preview propose the next level. It refuses a case that is not `proposed` or whose run is not
  `approved` (409), and refuses a repeat mark-sent (409). Actual delivery evidence (the letter
  itself, postal/e-mail proof) stays M16-02, still open; this only records that a person marked
  the case sent, by which channel and when.
* M16-06 closed (25.09.2026): CRM UI under `apps/web-crm/src/app/(app)/buchhaltung/mahnwesen/`.
  `einstellungen/page.tsx` + `DunningSettingsForm.tsx`: ladder editor (days, text, nullable fee
  per level), `fee_from_level`, "Vorschlagswerte laden" (`POST .../presets`), interest section
  (base rate field labelled "vom Betreiber zu pflegen", spread with Verbraucher/Unternehmer
  preset buttons that only set the spread field locally, no defaults persisted; the "Verzugszins
  aktiv" checkbox stays disabled until a base rate is entered). `[runId]/page.tsx` +
  `DunningCaseActions.tsx`: "Als versendet markieren" (channel select) on a proposed case, and
  "Mahnbescheid vorbereiten" plus a client-side JSON download of the preparation, both only
  rendered once the case's level reaches the highest configured level (looked up from the
  tenant-wide `dunning-settings`, see open point below). BFF allowlist extended
  (`apps/web-crm/src/app/api/bff/[...path]/route.ts`) for `GET`/`PUT
  accounting/dunning-settings`, `POST accounting/dunning-settings/presets`, `POST
  accounting/dunning-cases/{id}/mark-sent` and `POST`/`GET
  accounting/dunning-cases/{id}/mahnbescheid-vorbereitung`. These four endpoints are not yet in
  `apps/api/openapi.json` (not regenerated in this change per task scope), so the two server
  pages use the untyped `serverFetch` instead of the generated `serverApi()` client for exactly
  those calls; regenerating the client is a follow-up, not required for the pages to work.

## Result

`alembic upgrade head` (single head, revision 0072) and
`tests/integration/test_m16_dunning.py` both run and pass (3 passed): locks/preview/approval
(unchanged behaviour), presets plus fee posting with the draft receivable and the draft HVM
outgoing invoice for a non-HVM claim holder (WEG), Mahnbescheid preparation including
repeat-safe `GET`, and mark-sent advancing a two-level ladder from level 1 to level 2 with the
level 2 fee applied. `tests/integration/test_m13_receivables.py` and
`tests/integration/test_m14_invoices.py` re-run green to check for regressions in adjacent
accounting code. CRM: `pnpm vitest run src/components/accounting
"src/app/api/bff/[...path]/route.test.ts"` — 8 files, 105 tests passed (new:
`DunningSettingsForm.test.tsx`, `DunningCaseActions.test.tsx`; the BFF route test's stale case
that expected `PUT accounting/dunning-settings` to be refused was moved into the allowlisted
set together with the four new operations). `tsc --noEmit` and `eslint` clean for all files
touched in this change (pre-existing failures elsewhere, e.g. `FinApiConnections.test.tsx`, are
unrelated and untouched).

## Open points

M16-01 to M16-03 (updated), assumption A-029, plus:

* M16-07 closed (26.09.2026, A31): `POST /dunning-cases/{id}/mahnbescheid-preview` renders the
  preparation as a PDF on the tenant letterhead (`dunning_letters.build_mahnbescheid`: notice
  "Vorbereitung, Prüfung durch Rechtsanwalt erforderlich, kein Antrag", creditor, debtor, claim
  table with the main claim per item and due date, Nebenforderungen only when posted, dunning
  history), `POST /dunning-cases/{id}/mahnbescheid` files it as a generated document
  (`document_id` in the response; the prep row has no document column, which would need a
  migration). The JSON data set stays available. CRM: "Vorbereitung als PDF (kein Antrag)" and
  "PDF ablegen" next to the JSON download.
* A33 (26.09.2026): text modules per level in `dunning_letters.STANDARD_TEXTS` (neutral, no
  default assertion, no legal consequences), claim table (Posten, Fälligkeit, Betrag, Summe) in
  every letter via `mhvp.documents.letters.LetterTable`, placeholders `{frist}` (only with
  `payment_days`, M16-12), `{bankverbindung}` (only with a released account, none stored yet,
  M16-13), `{gesamtbetrag}`, `{forderungsinhaber}`, `{objekt}`, `{stufe}` in `letter_text`;
  unknown placeholders are refused (422). `POST /dunning-settings/letter-preview` renders a
  level with sample items for the settings form ("Vorschau" per level).
* M16-08: the "own receivable" branch (claim holder is HVM itself, `LegalEntityKind.MANAGER`)
  is covered by the test only by inserting HVM's `LegalEntity`/`Ledger` directly in the test
  setup (`_seed_manager_ledger`), because no HTTP endpoint creates a `MANAGER` legal entity yet
  (only HOA/RENTAL_OWNER/SEV_OWNER are created through the property flow today). A rent/tenant
  case (contract kind `tenancy`) was not covered by a dedicated test either; the code path is
  the same as for the WEG case (fee posted at the debtor's account inside the claim holder's
  ledger). Creating HVM's own legal entity/ledger through the platform (a one-off operator
  setup step) is a separate, small follow-up.
* M16-10 closed (26.09.2026): every case in a run response carries `highest_level` of the
  ladder that applies to its ledger's property; the run detail page uses it.
* M16-11 closed (26.09.2026): object overrides with field level inheritance (migration 0078,
  `docs/rules/M16-02.md`): an object row stores only what it overrides (`levels`,
  `threshold_amount`, `fee_from_level`, `interest_enabled`, `interest_base_rate`,
  `interest_spread`; NULL inherits), a level entry may leave out `text`, `fee_amount`,
  `payment_days`, `letter_text` to inherit them from the tenant level with the same number.
  `GET /dunning-settings?property_id=` returns effective values plus `sources` and `own`,
  `GET /dunning-settings/overrides` lists objects with an override, `DELETE` removes one. CRM:
  `buchhaltung/mahnwesen/einstellungen?objekt=<id>` with a property picker and per field
  "vom Mandanten erben" switches.
* M16-02 partially (26.09.2026): `mhvp.accounting.dunning_letters` renders the letter as a
  PDF draft on the tenant letterhead (`mhvp.documents.letters`, DIN 5008 form B; sender is the
  managing company from the tenant settings, the claim holder is named "im Auftrag von").
  Fee, interest and payment deadline appear only from configured values; the info block reads
  "Entwurf, kein Versand". `POST /dunning-cases/{id}/letter-preview` returns the PDF,
  `POST /dunning-cases/{id}/letter` files it (`letter_document_id`), `POST
  /dunning-cases/{id}/letter/send` requires G1 and then still refuses (409). CRM: button
  "Mahnschreiben (PDF-Entwurf)" on the run page. Open: text modules per level with statements
  on default (M16-02), payment deadline values (M16-12), bank account in the letter (M16-13).
  Update 26.09.2026 (A33): standard text modules per level and the claim table exist (see
  above); still open are the operator's approval of the wording, deadline values and the
  bank account.
  Update 26.09.2026 (M16-14, M16-12): level 1 (Zahlungserinnerung) is hard locked to no fee
  and no interest: `PUT /dunning-settings` rejects `fee_from_level` 1 and a non zero level 1
  fee (422, German detail), interest is only computed from level 2, and the run resets any
  level 1 fee or interest to 0.00 with a note in the case reason (`dunning.reminder_guard`,
  run totals `reminder_guard`). Default payment deadlines 14, 10, 7 days for levels 1 to 3
  (presets and new levels in the form), editable per tenant and per object override; no
  migration (`payment_days` lives in the `levels` JSON). BFF now allows `DELETE
  /accounting/dunning-settings` (removing an object override). Unit tests:
  `tests/unit/test_m16_dunning_reminder.py`.

## Betreiberentscheidung 25.09.2026 (V7, teilweise)

Stufenleiter und Ablauf sind entschieden (siehe docs/OPEN_QUESTIONS.md V7): Erinnerung
kostenlos, Gebühr ab 1. Mahnung ohne festgelegten Betrag, Verzugszins nach gesetzlicher Regel
mit vom Betreiber gepflegtem Basiszinssatz, automatische Entwürfe mit Freigabe, Gebühren als
Sollstellung und HVM-Rechnung, danach Vorbereitung Mahnbescheid. Klarstellung 25.09.2026: die
Gebühr wird als Sollstellung auf dem Debitorenkonto des Schuldners innerhalb des Buchungskreises
des Forderungsinhabers gebucht (bei Hausgeld: Forderungsinhaber die WEG, Schuldner der
Eigentümer; bei Miete: Forderungsinhaber der Vermieter/Eigentümer, Schuldner der Mieter); die
HVM-Rechnung über die Gebühr geht an den Forderungsinhaber (WEG bzw. Vermieter/Eigentümer), da
dieser die Gebühr bei seinem Schuldner einzieht und an die HVM zahlt. Umsetzung: Vorschlagswerte,
Gebühren- und Zinslogik nur mit gepflegten Werten, alles hinter G1 und Vier-Augen-Freigabe.

## A32 (26.09.2026): Rechtsträger der verwaltenden Gesellschaft über die Plattform

* `mhvp/tenant/manager_entity.py` and `mhvp/tenant/routers.py`: `GET /api/v1/tenant/manager-entity`
  (status `eingerichtet` or `nicht_eingerichtet`, permission `tenant_settings:read`) and
  `POST /api/v1/tenant/manager-entity` (permission `tenant_settings:update`): creates the
  `manager` legal entity (name from `tenant_settings.company.name`, else the tenant name) and
  its ledger with the default chart of accounts (template rows that apply to `manager`),
  idempotent; an empty ledger from an earlier direct seed only gets the accounts. Event
  `manager_entity.created`. No migration (the `manager` kind and a legal entity without
  property already existed).
* CRM: card "Rechtsträger der verwaltenden Gesellschaft" under Einstellungen, Mandant
  (`components/settings/ManagerEntitySetup.tsx`, status badge and setup button).
* Test `test_m16_dunning.py::test_a32_manager_entity_setup_and_tenancy_fee`: setup twice
  (same ids), status, 403 for the accountant role, rent case (`tenancy`, run scope
  `property`): fee draft receivable in the landlord's ledger, draft HVM outgoing invoice
  with the landlord (rental owner entity) as recipient and the manager ledger as issuer.
  The earlier `_seed_manager_ledger` in the same module stays as it was (direct seed);
  the endpoint completes such a ledger with the default accounts.
* M16-08 moved to docs/OPEN_QUESTIONS.md (partly solved; fiscal year and opening balances
  of the manager ledger stay open).

## Stand 26.09.2026

Zusammenfassung aus den Nachträgen dieses Plans, dem `CHANGELOG.md` (1.19.0 bis 1.22.1) und der Lückenliste `docs/plans/LUECKENLISTE-2026-09-26.md`; keine neuen Sachverhalte.

* Im Code: Mahnlauf mit Vorschau, Sperren und Freigabe, Stufenleiter mit Vorschlagswerten (V7), Gebühr als Sollstellung und HVM-Rechnungsentwurf, Objektüberschreibungen mit Vererbung (Regel M16-02, Migration 0078), Mahnschreiben und Mahnbescheid-Vorbereitung als PDF-Entwurf (A31, A33, `dunning_letters.py`), Zahlungserinnerung immer ohne Gebühr und Zinsen mit Standardfristen 14, 10, 7 Tage (M16-12, M16-14), Rechtsträger der Verwaltung über die Plattform (A32), Test D40 und D52 mit Fallkennung (A08, A17).
* Tests: `test_m16_dunning.py` (inkl. D40, D52, A32), `test_m16_dunning_letters.py`, `tests/unit/test_m16_dunning_reminder.py`, Vitest `DunningSettingsForm`, `DunningCaseActions`.
* Offen: M16-01 bis M16-03 (Gebührenbeträge, Basiszinssatz, Zustellnachweis), M16-08 Rest (Geschäftsjahr und Eröffnungsbilanz des Verwalter-Buchungskreises), M16-13 (Bankverbindung im Schreiben), Wortlaut der Textbausteine durch den Betreiber. Versand bleibt gesperrt.
